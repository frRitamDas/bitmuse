name: Android CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: write

jobs:
  build-dev-debug:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v5
    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
    - name: Setup local.properties
      env:
        LASTFM_API_KEY: ${{ secrets.LASTFM_API_KEY }}
        LASTFM_SECRET: ${{ secrets.LASTFM_SECRET }}
      run: |
        printf 'LASTFM_API_KEY=%s\n' "$LASTFM_API_KEY" > local.properties
        printf 'LASTFM_SECRET=%s\n' "$LASTFM_SECRET" >> local.properties
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build Dev Debug APK
      run: ./gradlew assembleDevDebug
    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: pexpo-dev-debug
        path: app/build/outputs/apk/dev/debug/*.apk

  build-prod-release:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v5
    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
    - name: Setup local.properties
      env:
        LASTFM_API_KEY: ${{ secrets.LASTFM_API_KEY }}
        LASTFM_SECRET: ${{ secrets.LASTFM_SECRET }}
      run: |
        printf 'LASTFM_API_KEY=%s\n' "$LASTFM_API_KEY" > local.properties
        printf 'LASTFM_SECRET=%s\n' "$LASTFM_SECRET" >> local.properties
    - name: Setup release signing
      env:
        KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
        KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
      run: |
        set -euo pipefail
        valid_keystore=false
        if [ -n "$KEYSTORE_BASE64" ]; then
          if printf '%s' "$KEYSTORE_BASE64" | base64 --decode > bitchord-release.jks 2>/dev/null; then
            if [ -s bitchord-release.jks ] && [ -n "$KEYSTORE_PASSWORD" ] && [ -n "$KEY_ALIAS" ] && [ -n "$KEY_PASSWORD" ]; then
              if keytool -list -keystore bitchord-release.jks -storepass "$KEYSTORE_PASSWORD" >/dev/null 2>&1 && \
                 keytool -list -keystore bitchord-release.jks -storepass "$KEYSTORE_PASSWORD" -alias "$KEY_ALIAS" >/dev/null 2>&1; then
                valid_keystore=true
              fi
            fi
          fi
        fi
        if [ "$valid_keystore" = true ]; then
          echo 'Using configured release keystore.'
          {
            echo 'storeFile=bitchord-release.jks'
            printf 'storePassword=%s\n' "$KEYSTORE_PASSWORD"
            printf 'keyAlias=%s\n' "$KEY_ALIAS"
            printf 'keyPassword=%s\n' "$KEY_PASSWORD"
          } > keystore.properties
        else
          echo 'Configured release keystore unavailable; using temporary CI signing key for this build.'
          rm -f bitchord-release.jks
          CI_STORE_PASSWORD="$(openssl rand -hex 32)"
          keytool -genkeypair -v -storetype JKS \
            -keystore bitchord-release.jks \
            -alias bitchord-ci -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass "$CI_STORE_PASSWORD" -keypass "$CI_STORE_PASSWORD" \
            -dname 'CN=Pexpo CI, O=Pexpo, C=IN' >/dev/null
          {
            echo 'storeFile=bitchord-release.jks'
            printf 'storePassword=%s\n' "$CI_STORE_PASSWORD"
            echo 'keyAlias=bitchord-ci'
            printf 'keyPassword=%s\n' "$CI_STORE_PASSWORD"
          } > keystore.properties
        fi
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build Pexpo v1.5.3
      run: ./gradlew assembleProdRelease --stacktrace
    - name: Prepare release assets
      run: |
        set -euo pipefail
        mkdir -p release-assets
        cp app/build/outputs/apk/prod/release/app-prod-universal-release.apk release-assets/pexpo-1.5.3-universal.apk
        cp app/build/outputs/apk/prod/release/app-prod-arm64-v8a-release.apk release-assets/pexpo-1.5.3-arm64-v8a.apk
        cp app/build/outputs/apk/prod/release/app-prod-armeabi-v7a-release.apk release-assets/pexpo-1.5.3-armeabi-v7a.apk
        cp app/build/outputs/apk/prod/release/app-prod-x86_64-release.apk release-assets/pexpo-1.5.3-x86_64.apk
        sha256sum release-assets/*.apk > release-assets/SHA256SUMS.txt
    - name: Upload Pexpo v1.5.3 artifact
      uses: actions/upload-artifact@v4
      with:
        name: pexpo-v1.5.3-release
        path: release-assets/*
    - name: Publish GitHub Release v1.5.3
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        set -euo pipefail
        if gh release view v1.5.3 --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
          echo 'v1.5.3 already published.'
          exit 0
        fi
        gh release create v1.5.3 \
          --repo "$GITHUB_REPOSITORY" \
          --target main \
          --title 'Pexpo Music v1.5.3' \
          --notes 'Pexpo Music v1.5.3: fixed account/profile switcher, working Add account and Manage accounts, duplicate Sign out, Replay persistence safety, remaining BitChord Replay branding, and variant-aware in-app APK updates. Assets include universal, arm64-v8a, armeabi-v7a and x86_64 builds.' \
          release-assets/pexpo-1.5.3-universal.apk \
          release-assets/pexpo-1.5.3-arm64-v8a.apk \
          release-assets/pexpo-1.5.3-armeabi-v7a.apk \
          release-assets/pexpo-1.5.3-x86_64.apk \
          release-assets/SHA256SUMS.txt
